# 05: 块快照存储加固：可注入存储、配额淘汰与降级

**What to build:**
本地缓存写满时应用继续正常工作，不再抛异常打断编辑。空间不够时优先淘汰其他页面的旧缓存，保住当前正在编辑的页面。仍然放不下时降级为只缓存块的元信息、放弃缓存正文。完整背景见同目录
`spec.md`。

降级顺序的依据：服务端有永久持久化，正文丢了还能从服务端取回；块元信息丢了才会找不到块。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [x] 块快照模块的存储对象改为可注入，便于测试驱动异常路径
- [x] 快照写入失败不向外抛异常
- [x] 捕获到配额溢出时，按最后访问时间淘汰其他页面的快照并重试
- [x] 重试仍失败时降级为只写块元信息、不写正文
- [x] 读到损坏内容时返回「无快照」而不是抛异常
- [x] 有测试：注入一个写入时抛配额异常的存储对象，分别断言不外抛、触发淘汰、重试、降级四条路径
- [x] 有存取往返测试：写入后读出的结构一致
- [x] 类型检查通过、测试通过
- [ ] 实机验证：把本地存储灌到接近上限后继续编辑，编辑过程不中断

## Comments

由 grok lane 实现（`grok_session_id` 3a8959ce-0add-40e3-b443-eb23a3f991be）。

落地：

- `src/blockModeSync.ts`：`saveBlockSnapshot` / `loadBlockSnapshot`
  新增可选第三参数 `storage`，默认 `window.localStorage`
  且只在调用时解析（模块导入阶段不碰 `window`，测试可完全绕开）。导出结构类型
  `SnapshotStorage`。
- `BlockSnapshot` 新增可选
  `lastAccessedAt`，保存时盖章。淘汰按它排序，缺失视为最旧。
- 配额溢出→按最后访问时间淘汰其他页面快照并重试→仍失败则只写元信息（`content`
  置空串，不删字段）。非配额错误一律吞掉且不触发淘汰。
- 解析在保存与读取两侧都原样透传块上的额外字段，为票 06 的布局字段留路。
- 新增 `scripts/blockModeSync.test.ts`（13 例）。

架构侧核验：

- 淘汰候选键在任何 `removeItem` 之前一次性收集，不存在 `storage.key(i)`
  索引位移问题。
- 淘汰循环带 `evicted` 集合，删不动时跳出，不会死循环。
- 四个变异体全部被测试捕获：去掉快照键前缀守卫、去掉「不淘汰当前页」守卫、LRU 比较方向反向、降级时不清空正文。
- `src/BlockPageView.tsx` 零 diff。

已知取舍：元信息覆盖仍触配额时会先删当前键再写。若这一步也失败，当前页快照整体消失，下次首屏没有缓存内容可渲染。快照是缓存、服务端有权威副本，接受。

待人工验收：把本地存储灌到接近上限后继续编辑，确认编辑不中断。
