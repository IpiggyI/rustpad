# 02：将堆叠展示的块正文高度统一调整为 200 像素

**What to build:**
堆叠展示使用更紧凑的块正文高度，同时完成已有页面的一次性高度调整，让用户看到更多块，并继续保留升级后的手动调整。

**Blocked by:** 无，可立即开始。

**Status:** ready-for-agent

- [x] 新块默认正文高度为 200 像素，不含块头部，仍支持拖动调整高度。
- [x] 已有页面首次完成升级调整时，所有已有块的正文高度统一为 200 像素，包括已保存的手动高度和当前整块折叠的块。
- [x] 每个页面只调整一次。之后手动改变高度，刷新、关闭重开或另一设备打开页面都不得再次重置为 200 像素。
- [x] 高度继续按页面共享；升级完成后，另一设备看到相同高度，不能依据自己的旧缓存再次执行迁移。
- [x] 调整只改变块高度及必要的升级记录，不改变正文、标题、语言、顺序、整块折叠或块内折叠。
- [x] 升级记录经过现有块清单读写和规范化后仍能保留；不得把首屏缓存或尚未就绪的清单误认为需要重新迁移的权威记录。
- [x] 空页面也能完成升级处理；以后添加的块使用新默认值，不因升级而自动增加块。
- [x] 测试覆盖有无旧高度、重复加载、旧缓存和第二设备打开；真实浏览器验证正文区域尺寸及升级后拖动再重开的高度保留。

## Comments

实现于 `f956be4`，浏览器验收于 `5683509`。

迁移标记是清单根字段 `compactHeights`，决策见
[ADR 0005](../../../docs/adr/0005-empty-manifest-is-a-real-page-state.md)。初始化判断抽成无 React 的
`src/manifestInit.ts`，顺序守卫用 `onContentReady`
作为首轮完整历史回放的一次性信号。

验证方式：`npm test` 173 项通过；`npm run test:browser`
中本票 7 项通过。四处变异：忽略标记、不写标记、跳过回放守卫三处被节点测试抓到（分别失败 4、10、2 项）；第四处"组件丢掉快照标记"节点测试抓不到，因此补了直接断言 localStorage 快照的浏览器检查，删掉两条写入路径中的任一条会使套件分别掉到 1 项和 0 项。

范围外但已一并落地：`decideManifestInit` 现在接受 `blocks: []`
的合法清单，这原属票 06 的判断。

未验证：所谓"第二设备"用的是同一浏览器的第二个上下文（各自独立的 localStorage），不是第二台物理设备；跨设备同步本身依赖服务端，未在真实双设备上验证。
