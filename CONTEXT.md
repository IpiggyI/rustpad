# Rustpad

一个协同文本编辑器。本仓库在上游 Rustpad 之上增加了「分块工作区」：把一个页面拆成多个可独立编辑的块。

## 术语

### 协同层

**协同文档**：服务端上的一份可被多个客户端同时编辑的文本，由操作变换（OT）保证一致。每份协同文档有一个字符串 id。

_避免_：房间、文件、doc

**OT 客户端**：把本地编辑转成 OT 操作、并把远端操作应用到本地的对象。仓库里有两个：接管 Monaco 编辑器的
`Rustpad`，以及不带界面、直接读写文本的 `RustpadHeadless`。

_避免_：连接、socket、同步器

### 分块工作区

**分块工作区**：本仓库特有的编辑模式，一个页面包含多个可独立编辑的块。与之相对的是单文档模式（`SingleDocView`）。

_避免_：多文件模式、分屏、面板

**单块展示**：分块工作区中一次只展示当前块正文的展示方式。

_避免_：单文档模式、普通模式

**堆叠展示**：分块工作区中按块顺序连续排列各块的展示方式。

**当前块**：当前设备在页面中选定的块；单块展示时，它是正文区域展示的块。

_避免_：当前页面、当前文档

**页面**：分块工作区的最小单位，由 URL 哈希里的页面 id 标识（代码里是
`pageId`）。一个页面拥有一份块清单和若干个块。

_避免_：文档、房间、工作区

**块**：页面里一段可独立编辑的内容，有自己的标题、语言和一份协同文档。代码里的条目类型是
`BlockInfo`，界面组件是 `BlockEditor`。

_避免_：分组块、片段、单元格、section

**块 id**：块在页面内的唯一标识，由 `generateBlockId` 生成的 6 位小写字母数字。

_避免_：块名、索引

**块清单**：记录一个页面里有哪些块、顺序如何、每个块的元信息是什么的那份数据。它本身也是一份协同文档。代码里的类型是
`Manifest`，同步钩子是 `useManifest`。

_避免_：目录、索引、块列表、大纲

**布局状态**：描述块「怎么显示」而不是「内容是什么」的数据：块高度、块是否折叠、块内 markdown 标题的折叠情况。与块的正文分开存放。

_避免_：视图状态、UI 状态、偏好

**块快照**：块清单与块正文在浏览器本地存储里的镜像，用于在连上服务端之前先渲染出内容。类型是
`BlockSnapshot`。它是缓存，不是数据源。

_避免_：本地存储、草稿、离线副本、备份
